# Copy this file to `.env` and replace the example IP with the
# local IPv4 address of the computer running the Spring Boot backend.
#
# The phone and computer must normally be connected to the same network.
EXPO_PUBLIC_API_URL=http://192.168.1.100:8080
