# Copy to `.env` when environment variables are needed.
# Never commit the real `.env`.

SPRING_APPLICATION_NAME=kai-pool-backend
SERVER_PORT=8080

# Add database credentials only after the database is selected.
# SPRING_DATASOURCE_URL=
# SPRING_DATASOURCE_USERNAME=
# SPRING_DATASOURCE_PASSWORD=

# Add AI provider keys only after the provider is selected.
# OPENAI_API_KEY=
# GEMINI_API_KEY=
# GROQ_API_KEY=
